#include <stdio.h>

/*
 * server main
 * -----------
 * Phase 2 server placeholder.
 * The server will call the shared shell_core module once socket
 * communication is introduced in later branches.
 */
int main(void) {
    printf("Phase 2 server stub ready.\n");
    return 0;
}
